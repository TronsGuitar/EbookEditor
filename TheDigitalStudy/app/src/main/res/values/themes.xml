<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose handles all theming; this is a minimal XML shell for the window -->
    <style name="Theme.TheDigitalStudy" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/paper_white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
